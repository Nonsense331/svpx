%h2=@channel.title
-videos = @channel.videos.where(series_id: nil, watched: false).order('created_at DESC')
-videos.each do |video|
  .video-container
    =render partial: 'home/video', locals: {video: video}